import { ParentSettingsPageClient } from '@/components/parent/settings/ParentSettingsPageClient';

export const metadata = {
  title: 'Cài đặt | EduLink',
  description: 'Quản lý hồ sơ và mật khẩu phụ huynh',
};

export default function ParentSettingsPage() {
  return <ParentSettingsPageClient />;
}
